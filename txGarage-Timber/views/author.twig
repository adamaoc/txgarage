{% set category = function('pageCatClass', wp_title) %}

<div class="hero-wrap hero-wrap--inner insta-box">
	<div class="insta-box__wrapper">
		<div id="instafeed"></div>
		<div class="insta-box__{{category}}"></div>
	</div>
	<h1>{{ title }}</h1>
</div>

{% include "components/author-byline.twig" %}

<section class="post-list--expanded limit-wrapper">
{% for post in posts %}
  {% set category = function('catClass', post.categories) %}
  {% include "tz-archive.twig" %}
{% endfor %}
{% include "components/pagination.twig" %}
</section>
